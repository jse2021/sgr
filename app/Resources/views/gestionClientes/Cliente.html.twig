{% extends 'base.html.twig' %}
{% block titulo %}
  Clientes {% endblock %}

{% block contenido %}
  <h1 class="mt-4 mb-3">
      Clientes
    </h1>


    <div class="row">
      <div class="col-md-2"></div>
      <div class="col-md-8">

        <table class="table">
          <thead class="thead-light">
            <tr>
              <th scope="col">Editar</th>
              <th scope="col">Dni</th>
              <th scope="col">Nombre</th>
              <th scope="col">Apellido</th>
              <th scope="col">Email</th>
              <th scope="col">Celular</th>
              <th scope="col">Borrar</th>
            </tr>
          </thead>
          
          {# {# <tbody> #}


            {# {% for clientes in cliente %} #}
            
                {# {% if clientes.dni is defined %} #}
                        <tr>
                        {# es el id que va a recibir el controlador de reservas, para que luego traiga los
                        datos al dar editar una reserva #}
                            {# <th scope="row"><a href="{{path('nuevaReserva',{'reserva':reserva.id}) }}"><i class="fas fa-pen"></i></a></th> #}
                
                            <th scope="row"><a href="{{path('nuevoCliente',{'dni':cliente.dni})}}"></a>xxxxxxxxxxxxxx</th> 
                             <td>{{cliente.dni}}</td> 
                            <td>{{cliente.nombre}}</td> 
                            <td>{{cliente.apellido}}</td> 
                            <td>{{cliente.email}}</td> 
                            <td>{{cliente.celular}}</td> 
                            {# esta opcion, realiza el borrado de una reserva, pasa por parametro a la funcion borrarReserva el id de la reserva
                            y la fecha para el popup #}
                            {# <th scope="row"><a href="#" onClick="return controlBorrado('{{path('borrarReserva',{'id':reserva.id})}}','{{reserva.fecha|date('Y/m/d h:i')}}');"><i class="far fa-trash-alt"></i></a></th> #}
                        </tr>
                        {# {% endif %} #}
            {# {% endfor %} #}
          </tbody>
        </table>

        {# <div class="col-md-2"></div>  #}

      </div>
    </div>
  </div>
{% endblock %}
